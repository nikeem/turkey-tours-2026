# 🏖️ Лендинг "Туры в Турцию 2026 - Раннее бронирование"

Современный, конверсионный лендинг для сбора заявок на подбор туров в Турцию с акцией раннего бронирования на 2026 год.

## 📋 Структура лендинга

Лендинг включает следующие секции:

1. **Hero-секция** - привлекающий внимание первый экран с ключевыми преимуществами
2. **Статистика** - социальное доказательство (числа и факты)
3. **Что такое раннее бронирование** - объяснение акции с 3 этапами
4. **Преимущества** - 6 ключевых выгод раннего бронирования
5. **Курорты Турции** - 6 популярных направлений
6. **Форма подбора тура** - конверсионная форма заявки
7. **Что входит в тур** - прозрачность включённых услуг
8. **Популярные отели** - 6 карточек отелей с ценами
9. **Таблица цен** - цены по месяцам
10. **Как работает** - 3 простых шага бронирования
11. **FAQ** - аккордеон с частыми вопросами
12. **Срочность (FOMO)** - призыв к действию
13. **О нас** - доверие и авторитет
14. **Контакты** - способы связи
15. **Footer** - подвал с навигацией

## 🚀 Как запустить

### Вариант 1: Простой запуск (без сервера)

Просто откройте файл `index.html` в браузере:

```bash
# Mac
open index.html

# Linux
xdg-open index.html

# Windows
start index.html
```

### Вариант 2: Локальный сервер

Для более корректной работы рекомендуется использовать локальный сервер:

```bash
# Python 3
python3 -m http.server 8000

# Node.js (с установленным http-server)
npx http-server

# PHP
php -S localhost:8000
```

Затем откройте в браузере: `http://localhost:8000`

## 🎨 Кастомизация

### Изменение цветов

Откройте файл `styles.css` и измените CSS переменные в `:root`:

```css
:root {
    --primary-color: #0066cc;      /* Основной синий */
    --secondary-color: #00a8e8;    /* Дополнительный */
    --accent-color: #ff6b35;       /* Оранжевый для CTA */
    --text-dark: #333;
    --text-light: #666;
    --white: #ffffff;
    --light-gray: #f5f5f5;
}
```

### Изменение текста

Все тексты находятся в `index.html`. Найдите нужный текст с помощью поиска (Ctrl+F / Cmd+F) и измените его.

### Изменение контактов

Найдите секцию `<section class="contact">` в `index.html` и измените:

```html
<a href="tel:+79991234567">+7 (999) 123-45-67</a>
<a href="mailto:info@turkey-tours.ru">info@turkey-tours.ru</a>
```

### Изменение отелей

Найдите секцию `<section class="hotels">` и отредактируйте карточки отелей. Каждая карточка находится в `<div class="hotel-card">`.

### Интеграция формы

Для реальной работы формы вам нужно:

1. Создать endpoint на сервере для приёма заявок
2. В файле `script.js` найдите функцию `submitForm` и раскомментируйте код:

```javascript
fetch('/api/submit-booking', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify(data)
}).then(response => response.json())
.then(result => {
    showModal();
    form.reset();
});
```

3. Замените `/api/submit-booking` на ваш реальный endpoint

## 📱 Адаптивность

Лендинг полностью адаптивен и работает на:
- 📱 Мобильные телефоны (от 320px)
- 📱 Планшеты (768px+)
- 💻 Ноутбуки и десктопы (968px+)

## 🎯 Ключевые особенности

### Конверсионные элементы
- **Множественные CTA** - кнопки призыва к действию в каждой секции
- **Форма заявки** - простая форма с минимальным количеством полей
- **Социальное доказательство** - статистика, рейтинги, отзывы
- **FOMO-элементы** - срочность, ограничение по времени
- **FAQ** - снятие возражений

### SEO-оптимизация
- Мета-теги (title, description, keywords)
- Семантическая HTML-разметка
- Правильные заголовки (h1, h2, h3)

### Производительность
- Ленивая загрузка изображений (lazy loading)
- Оптимизированные CSS и JS
- Минимальное количество внешних зависимостей

### Анимации
- Плавный скролл
- Анимация появления элементов при скролле
- Hover-эффекты на карточках
- Интерактивный аккордеон в FAQ

## 📊 Интеграция аналитики

### Яндекс.Метрика

Добавьте перед закрывающим тегом `</head>` в `index.html`:

```html
<!-- Yandex.Metrika counter -->
<script type="text/javascript" >
   (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   m[i].l=1*new Date();k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
   (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

   ym(XXXXXXX, "init", {
        clickmap:true,
        trackLinks:true,
        accurateTrackBounce:true,
        webvisor:true
   });
</script>
<noscript><div><img src="https://mc.yandex.ru/watch/XXXXXXX" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
<!-- /Yandex.Metrika counter -->
```

Замените `XXXXXXX` на ваш номер счётчика.

### Google Analytics

Добавьте перед закрывающим тегом `</head>`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

Замените `G-XXXXXXXXXX` на ваш идентификатор.

## 🔔 Цели для аналитики

Добавьте отслеживание целей в `script.js`:

```javascript
// Пример для Яндекс.Метрики
function submitForm(event) {
    event.preventDefault();

    // Отправка цели
    if (typeof ym !== 'undefined') {
        ym(XXXXXXX, 'reachGoal', 'form_submit');
    }

    // ... остальной код
}
```

## 📦 Структура файлов

```
/
├── index.html          # Основной HTML файл
├── styles.css          # Стили
├── script.js           # JavaScript функционал
└── README.md           # Этот файл
```

## 🛠️ Технологии

- **HTML5** - Семантическая разметка
- **CSS3** - Современные стили с Flexbox и Grid
- **JavaScript (ES6+)** - Интерактивность
- **CSS Variables** - Лёгкая кастомизация цветов
- **Intersection Observer API** - Анимации при скролле

## 🌐 Кроссбраузерность

Лендинг протестирован и работает в:
- ✅ Chrome/Edge (последние 2 версии)
- ✅ Firefox (последние 2 версии)
- ✅ Safari (последние 2 версии)
- ✅ Opera (последние 2 версии)

## 📝 Лицензия

Этот проект создан для демонстрационных целей. Вы можете свободно использовать и модифицировать его.

## 🤝 Поддержка

При возникновении вопросов или проблем:
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что все файлы (html, css, js) находятся в одной папке
3. Проверьте, что открываете файл через локальный сервер, а не напрямую (file://)

## ✨TODO: Возможные улучшения

- [ ] Добавить слайдер с отзывами клиентов
- [ ] Интегрировать интерактивную карту курортов
- [ ] Добавить калькулятор стоимости тура
- [ ] Интегрировать CRM-систему
- [ ] Добавить A/B тестирование вариантов CTA
- [ ] Создать версия на английском языке

---

**Создано с ❤️ для конверсионного маркетинга**
