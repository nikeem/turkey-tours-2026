# 🏖️ Лендинг "Туристическая компания" - Версия 2

Альтернативная версия современного, конверсионного лендинга для онлайн-подбора туров с **системой переменных для лёгкой кастомизации**.

## 📋 Структура лендинга

Лендинг включает следующие секции:

1. **Header** - Фиксированная шапка с логотипом, навигацией и телефоном
2. **Hero** - Главный экран с формой быстрого подбора тура
3. **Горящие туры** - Слайдер с топ-предложениями и скидками
4. **Минимальные цены** - Цены "от" по популярным направлениям
5. **Почему мы** - 6 ключевых преимуществ компании
6. **Отзывы** - Слайдер с отзывами клиентов
7. **Как работает** - 5 шагов бронирования тура
8. **Форма заявки** - Контактная форма с валидацией
9. **Footer** - Подвал с контактами и соцсетями

## 🚀 Как запустить

### Вариант 1: Простой запуск (без сервера)

Просто откройте файл `index2.html` в браузере:

```bash
# Mac
open index2.html

# Linux
xdg-open index2.html

# Windows
start index2.html
```

### Вариант 2: Локальный сервер

Для более корректной работы рекомендуется использовать локальный сервер:

```bash
# Python 3
python3 -m http.server 8000

# Node.js
npx http-server

# PHP
php -S localhost:8000
```

Затем откройте: `http://localhost:8000/index2.html`

---

## 🎨 Кастомизация через переменные

### ⚡ Быстрая настройка (изменить в одном месте!)

Все текстовые переменные находятся в **файле `script2.js`** в объекте `CONFIG.data`:

```javascript
const CONFIG = {
    data: {
        // Бренд и контакты
        brand_name: 'TravelMaster',
        phone: '+79991234567',
        phone_display: '+7 (999) 123-45-67',
        email: 'info@travelmaster.ru',
        address: 'г. Москва, ул. Туристическая, д. 15',

        // Hero секция
        hero_title: 'Подберите идеальный тур онлайн за минуты',
        hero_subtitle: 'Лучшие цены от 500+ туроператоров...',
        hero_cta_button: 'Найти тур',

        // ... и так далее
    }
};
```

### 🎨 Изменение цветов

Откройте файл [styles2.css](styles2.css) и измените CSS-переменные в `:root`:

```css
:root {
    /* Цвета бренда */
    --primary-color: #2563eb;        /* Основной синий */
    --secondary-color: #f59e0b;      /* Оранжевый для акцентов */

    /* Фоны */
    --bg-dark: #1e293b;              /* Тёмный фон */
    --bg-light: #ffffff;             /* Светлый фон */
    --bg-gray: #f8fafc;              /* Серый фон */

    /* Текст */
    --text-primary: #1e293b;         /* Основной текст */
    --text-secondary: #64748b;       /* Вторичный текст */
}
```

---

## 📝 Полный список переменных

### 🏢 Бренд и контакты

| Переменная | Описание | Пример |
|------------|----------|--------|
| `{{brand_name}}` | Название бренда | TravelMaster |
| `{{phone}}` | Телефон (ссылка) | +79991234567 |
| `{{phone_display}}` | Телефон (отображение) | +7 (999) 123-45-67 |
| `{{email}}` | Email | info@travelmaster.ru |
| `{{address}}` | Адрес компании | г. Москва, ул. Туристическая, 15 |
| `{{work_hours}}` | Режим работы | Пн-Вс: 9:00 - 21:00 |
| `{{current_year}}` | Текущий год | 2026 |

### 🎯 Hero секция

| Переменная | Описание |
|------------|----------|
| `{{hero_title}}` | Главный заголовок |
| `{{hero_subtitle}}` | Подзаголовок |
| `{{hero_cta_button}}` | Текст на кнопке |
| `{{hero_note}}` | Примечание под формой |

### 🔥 Горящие туры

| Переменная | Описание |
|------------|----------|
| `{{hot_tours_title}}` | Заголовок секции |
| `{{hot_tours_subtitle}}` | Подзаголовок |
| `{{hot_tours_cta}}` | Текст кнопки "Показать все" |

#### Карточки туров (для каждого тура):

| Переменная | Описание | Пример |
|------------|----------|--------|
| `{{tour_X_country}}` | Страна | Турция |
| `{{tour_X_resort}}` | Курорт/отель | Анталья, 5* отель |
| `{{tour_X_dates}}` | Даты | 15 - 22 января |
| `{{tour_X_duration}}` | Длительность | 7 ночей |
| `{{tour_X_price_old}}` | Старая цена | 85 000 ₽ |
| `{{tour_X_price}}` | Текущая цена | 59 990 ₽ |

*где X = 1, 2, 3, 4 (номер тура)*

### 💰 Минимальные цены

| Переменная | Описание |
|------------|----------|
| `{{prices_title}}` | Заголовок |
| `{{prices_subtitle}}` | Подзаголовок |
| `{{prices_note}}` | Примечание |

#### Карточки цен:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `{{price_X_country}}` | Страна | Турция |
| `{{price_X_value}}` | Цена "от" | 59 990 |
| `{{price_X_note}}` | Примечание | Анталья, Алания |

*где X = 1, 2, 3, 4, 5, 6*

### ⭐ Почему мы

| Переменная | Описание |
|------------|----------|
| `{{why_us_title}}` | Заголовок |
| `{{why_us_subtitle}}` | Подзаголовок |

#### Преимущества:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `{{benefit_X_title}}` | Заголовок преимущества | Опыт с 2010 года |
| `{{benefit_X_text}}` | Описание | Более 15 лет работы... |

*где X = 1, 2, 3, 4, 5, 6*

#### Гарантия:

| Переменная | Описание |
|------------|----------|
| `{{guarantee_badge}}` | Иконка/эмодзи | 🛡️ |
| `{{guarantee_text}}` | Текст гарантии | Полная гарантия возврата... |

### 💬 Отзывы

| Переменная | Описание |
|------------|----------|
| `{{reviews_title}}` | Заголовок |
| `{{reviews_subtitle}}` | Подзаголовок |

#### Отзывы:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `{{review_X_rating}}` | Рейтинг (звёзды) | ★★★★★ |
| `{{review_X_text}}` | Текст отзыва | Отличный отдых... |
| `{{review_X_name}}` | Имя клиента | Анна Петрова |
| `{{review_X_tour}}` | Тур | Турция, Анталья |

*где X = 1, 2, 3, 4, 5, 6*

### 🔄 Как работает

| Переменная | Описание |
|------------|----------|
| `{{how_title}}` | Заголовок |
| `{{how_subtitle}}` | Подзаголовок |

#### Шаги:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `{{step_X_title}}` | Заголовок шага | Подбор |
| `{{step_X_text}}` | Описание | Заполните форму... |

*где X = 1, 2, 3, 4, 5*

### 📧 Контактная форма

| Переменная | Описание |
|------------|----------|
| `{{contact_form_title}}` | Заголовок |
| `{{contact_form_text}}` | Описание |
| `{{contact_form_button}}` | Текст на кнопке |
| `{{contact_form_note}}` | Примечание |

### ✅ Модальное окно

| Переменная | Описание |
|------------|----------|
| `{{modal_title}}` | Заголовок |
| `{{modal_text}}` | Текст сообщения |

### 🦶 Footer

| Переменная | Описание |
|------------|----------|
| `{{footer_description}}` | Описание компании |

---

## 🔧 Изменение изображений

### Логотип

Найдите в HTML:

```html
<a href="#" class="logo">
    <span class="logo__text">{{brand_name}}</span>
</a>
```

Замените на изображение:

```html
<a href="#" class="logo">
    <img src="images/logo.png" alt="{{brand_name}}">
</a>
```

### Фон Hero-секции

В [styles2.css](styles2.css) найдите:

```css
.hero {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

Или в HTML замените `.hero__background` на своё изображение.

### Изображения туров

Найдите карточки туров в HTML и замените `src`:

```html
<img src="https://images.unsplash.com/photo-..." alt="Турция">
```

на

```html
<img src="images/turkey.jpg" alt="Турция">
```

### Аватары отзывов

Замените:

```html
<img src="https://i.pravatar.cc/100?img=1" alt="{{review_1_name}}">
```

на

```html
<img src="images/reviews/review-1.jpg" alt="{{review_1_name}}">
```

---

## 🔌 Интеграция с CRM/Email

### Вариант 1: Отправка на свой сервер

В файле [script2.js](script2.js) найдите функцию `initContactForm()` и раскомментируйте:

```javascript
fetch('/api/submit-booking', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
    },
    body: JSON.stringify(formData)
})
.then(response => response.json())
.then(result => {
    showModal();
    form.reset();
});
```

### Вариант 2: Google Sheets (без бэкенда)

1. Создайте Google Таблицу
2. Создайте Google Apps Script (Extensions → Apps Script)
3. Вставьте код:

```javascript
function doPost(e) {
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    var data = JSON.parse(e.postData.contents);
    sheet.appendRow([new Date(), data.name, data.phone, data.email, data.message]);
    return ContentService.createTextOutput(JSON.stringify({"result":"success"}))
      .setMimeType(ContentService.MimeType.JSON);
}
```

4. Deploy → Web app → Get URL
5. Вставьте URL в `script2.js` вместо `YOUR_GOOGLE_SCRIPT_URL`

### Вариант 3: Telegram-бот

Добавьте в `initContactForm()`:

```javascript
const botToken = 'YOUR_BOT_TOKEN';
const chatId = 'YOUR_CHAT_ID';
const message = `Новая заявка!\nИмя: ${name}\nТелефон: ${phone}\nEmail: ${email}\nСообщение: ${message}`;

fetch(`https://api.telegram.org/bot${botToken}/sendMessage?chat_id=${chatId}&text=${encodeURIComponent(message)}`);
```

---

## 📊 Интеграция аналитики

### Яндекс.Метрика

Добавьте перед `</head>` в [index2.html](index2.html):

```html
<!-- Yandex.Metrika counter -->
<script type="text/javascript" >
   (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   m[i].l=1*new Date();k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
   (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");

   ym(XXXXXXX, "init", {
        clickmap:true,
        trackLinks:true,
        accurateTrackBounce:true,
        webvisor:true
   });
</script>
<noscript><div><img src="https://mc.yandex.ru/watch/XXXXXXX" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
<!-- /Yandex.Metrika counter -->
```

Замените `XXXXXXX` на ваш номер счётчика.

### Google Analytics

Добавьте перед `</head>`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

### Отправка целей

В [script2.js](script2.js) в конце файла есть закомментированные примеры отправки целей. Раскомментируйте и настройте:

```javascript
// Яндекс.Метрика
function reachGoal(goal) {
    if (typeof ym !== 'undefined') {
        ym(YANDEX_METRIKA_ID, 'reachGoal', goal);
    }
}

// Использование при отправке формы
reachGoal('form_submit');
```

---

## 📱 Адаптивность

Лендинг полностью адаптивен:

- 📱 **Мобильные**: 320px - 768px
- 📱 **Планшеты**: 768px - 968px
- 💻 **Десктоп**: 968px+

### Точки адаптации в CSS

```css
/* Планшеты */
@media (max-width: 968px) { ... }

/* Мобильные */
@media (max-width: 768px) { ... }

/* Маленькие мобильные */
@media (max-width: 480px) { ... }
```

---

## 🎯 Функциональные элементы

### ✅ Реализовано

- ✅ Мобильное меню-гамбургер
- ✅ Слайдеры для туров и отзывов
- ✅ Форма с валидацией
- ✅ Модальное окно успешной отправки
- ✅ Плавный скролл по якорям
- ✅ Фиксированный header при скролле
- ✅ Автозаполнение формы при клике на "Забронировать"

### 🔄 Опционально (раскомментируйте)

- Анимации при скролле (`initScrollAnimations()`)

---

## 📦 Структура файлов

```
test2/
├── index2.html          # Основной HTML файл
├── styles2.css          # Стили с CSS-переменными
├── script2.js           # JavaScript с CONFIG
├── README_V2.md         # Этот файл документации
└── images/              # Папка для изображений (создайте самостоятельно)
    ├── logo.png
    ├── tours/
    └── reviews/
```

---

## 🛠️ Технологии

- **HTML5** - Семантическая разметка
- **CSS3** - Flexbox, Grid, CSS Variables
- **JavaScript (ES6+)** - Интерактивность
- **Intersection Observer API** - Для анимаций (опционально)

---

## 🌐 Кроссбраузерность

- ✅ Chrome/Edge (последние 2 версии)
- ✅ Firefox (последние 2 версии)
- ✅ Safari (последние 2 версии)
- ✅ Opera (последние 2 версии)

---

## 📝 Быстрый старт: 3 шага

### 1️⃣ Измените бренд и контакты

Откройте [script2.js](script2.js) и измените:

```javascript
brand_name: 'Ваш бренд',
phone: '+79999999999',
phone_display: '+7 (999) 999-99-99',
email: 'info@yourdomain.ru',
```

### 2️⃣ Измените цвета

Откройте [styles2.css](styles2.css) и изменьте:

```css
:root {
    --primary-color: #ff5722;  /* Ваш основной цвет */
    --secondary-color: #ffc107; /* Вторичный цвет */
}
```

### 3️⃣ Замените изображения

Замените URL изображений в HTML на свои:

```html
<img src="images/your-image.jpg" alt="Описание">
```

---

## 🆚 Сравнение с версией 1 (index.html)

| Характеристика | Версия 1 | Версия 2 (index2.html) |
|----------------|----------|------------------------|
| Переменные | ❌ Нет | ✅ Да (CONFIG в JS) |
| Форма подбора | ❌ Нет | ✅ Да |
| Слайдеры | ❌ Нет | ✅ Да (2 слайдера) |
| Мобильное меню | ❌ Нет | ✅ Гамбургер |
| Минимальные цены | ❌ Нет | ✅ Да |
| Шаги работы | ❌ Нет | ✅ Да (5 шагов) |
| Кастомизация | Ручная в HTML | Через переменные |
| Сложность edits | Выше | Ниже |

---

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте консоль браузера (F12 → Console)
2. Убедитесь, что все файлы в одной папке
3. Открывайте через локальный сервер, а не `file://`
4. Проверьте, что заменили все `{{variables}}`

---

## ✨ TODO: Возможные улучшения

- [ ] Добавить фильтры для карточек туров
- [ ] Интегрировать карту курортов
- [ ] Добавить калькулятор стоимости
- [ ] Создать версию на английском языке
- [ ] Добавить A/B тестирование
- [ ] Интегрировать онлайн-чат
- [ ] Добавить темную тему

---

## 📄 Лицензия

Этот проект создан для демонстрационных целей. Вы можете свободно использовать и модифицировать его.

---

**Создано с ❤️ для быстрого запуска туристического бизнеса**
